# Hoist all packages to root node_modules so that externalized native modules
# (like better-sqlite3) are resolvable from artifacts/api-server/dist/index.mjs
# at runtime. Without this, pnpm's strict isolation hides them from the bundle.
shamefully-hoist=true
